/* @flow */
/* eslint-disable no-unused-vars */

// Promiseable is a type that includes everything that Utils.returnPromise() can accept
type Promiseable = ?Promise<*>|Object|boolean;

type ActionProps = Object;
